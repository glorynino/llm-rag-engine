# llm-rag-engine
A lightweight local RAG-based chatbot system using open-source LLMs (such as Qwen) to analyze and query PDF and text documents. The project uses embeddings and FAISS for semantic search, enabling fast and efficient document retrieval on CPU-only machines.
